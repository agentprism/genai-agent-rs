[package]
name = "pi-ffi"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"
license = "MIT OR Apache-2.0"
repository = "https://github.com/agentprism/genai-agent-rs"
description = "Opaque C and generated Swift bindings for pi-ai and pi-agent-core."
publish = false

[lib]
crate-type = ["lib", "cdylib", "staticlib"]

[dependencies]
futures-util = { version = "0.3.31", default-features = false, features = ["alloc", "std"] }
pi-agent-core = { path = "../../crates/pi-agent-core" }
pi-agent-runtime-tokio = { path = "../../crates/pi-agent-runtime-tokio" }
pi-ai = { path = "../../crates/pi-ai" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
tokio = { version = "1.47.1", features = ["rt-multi-thread", "sync"] }
uniffi = { version = "0.32.0", default-features = false }
url = "2.5.7"

[dev-dependencies]
uniffi = { version = "0.32.0", features = ["cli"] }
