# pi-ai parity audit and remediation plan

- **Governing goal:** `docs/porting-pi-ai-and-agent-core-docs/goal.md` (read in full before synthesis).
- **Authority:** `/home/vikash/pi-pin-c49906ec7/packages/ai` at `c49906ec77788625aacbdc53ebca6fbe65bd20f5`.
- **Previous port pin:** `496185f6e4267b979e3663c45f7eb70b0c6a97b4`.
- **Audit date:** 2026-08-21.
- **Method:** static, source-anchored parity comparison. The scoped auditors read the listed pi and Rust files, inspected the complete pin diff, mapped README/test behavior, and deep-compared catalog artifacts where available. This synthesis accepts the 140 confirmed records, merges only obvious duplicates, independently judges the proposed cannot-preserve item, and adds it as F142 because it is implementable. No build or test was run in this read-only phase.

## Summary

- **Inputs:** 140 confirmed records, one refuted record (F068), and one cannot-preserve candidate. The candidate is achievable in Rust and is promoted to F142.
- **Consolidated findings:** 106 rows after duplicate merging. **47 blocking, 47 major, 12 minor.** Original IDs remain on every merged row.
- **Kinds:** 27 error-path deltas; 23 behavior deltas; 17 wire deltas; 13 missing features; 8 persisted-data deltas; 5 runtime impersonations; 5 non-idiomatic surfaces; 5 test gaps; 2 misleading-doc findings; 1 README-not-recreatable finding.
- **Headline gaps:** the event protocol omits every `partial` snapshot; core dynamic data is narrowed or JavaScript-emulated; current-pin reasoning metadata is persisted in a removed field; mandatory Rust SDKs are bypassed while OpenAI/Copilot identities are fabricated; provider hooks/transports, auth/OAuth, catalogs, Radius models, and error paths are observably different.
- **Cannot-preserve result:** none. A lossless UTF-16/WTF-8 newtype can preserve lone-surrogate values and UTF-16 slicing without impersonating a JavaScript runtime.

## Findings

Severity below uses the verifier severity. A merged ID cell means the records describe the same root observable defect.

| ID(s) | Severity | Kind | Title | pi | Rust | Evidence (short) | Remediation |
|---|---|---|---|---|---|---|---|
| F001, F002, F032, F054, F071, F079, F094, F103, F104, F124, F125, F128, F135 | blocking | wire-delta | Canonical nonterminal events omit `partial` and expose proxy-only fields | `types.ts:527-551`; `README.md:130-165,574-607`; provider emitters including `faux.ts:338-420`, `openai-completions.ts:336-615`, `openai-responses-shared.ts:456-737`, `anthropic-messages.ts:584-728`, `google-generative-ai.ts:95-211`, `google-vertex.ts:113-228`, `bedrock-converse-stream.ts:562-727` | `event_stream.rs:1-90,1027-1111`; emitters in `providers/faux.rs:489-634`, `api/openai_completions.rs:1079-1409`, `openai_responses_shared.rs:1311-1913`, `anthropic_messages.rs:1892-2128`, `google_generative_ai.rs:329-442`, `bedrock_converse_stream.rs:1246-1477`; `examples/quickstart.rs:99-139` | Pi carries the current `AssistantMessage` on start and all nine update events. Rust intentionally omits it and adds signatures, redaction, tool identity, and namespace at event top level; README partial-tool JSON requires a private reconstruction workaround. | Add `partial: Arc<AssistantMessage>` to every nonterminal variant, mutate then snapshot before every emission, remove all non-pi top-level fields, and make progressive tool arguments observable through `partial.content[contentIndex]`. Port exact wire and README tests across every provider. |
| F003 | blocking | missing-feature | Generic public `EventStream<T,R>` and its `push`/`end` contract are absent | `utils/event-stream.ts:3-88`; `index.ts:37-40` | `event_stream.rs:326-555`; `lib.rs:51` | Pi exposes a constructible generic stream, public push/end, async iteration, result, and a factory; `end(result)` may settle without a terminal event. Rust exposes a single-consumer assistant stream plus a separate sender. | Implement a shared-state generic `EventStream<T,R>` with public push/end/result and `Stream`, then specialize/factory it for assistant events. Pin queued draining, first-terminal-wins, ignored late pushes, explicit result settlement, and `end(None)` pending semantics. |
| F004, F056, F102, F121, F126, F129, F137 | blocking | persisted-data-delta | Current-pin `reasoning_details` persistence and replay migration is unported | `types.ts:356-364,427-447`; `openai-completions.ts:220-248,620-630,1233-1308`; `test/openai-completions-reasoning-details.test.ts:115-183` | `types.rs:1208-1254,2318-2402`; `openai_completions.rs:1008-1143,2173-2256,3604-3635,4509-4548` | Pi removed `AssistantMessage.reasoningDetails`, stores the ordered detail array in a thinking block's signature, and uses tool-call signatures only as legacy fallback. Rust persists and prefers the removed field and attaches new encrypted details to tool calls. | Remove the public/serde field; create or reuse a thinking block even with empty visible thinking; store the validated ordered array in `thinking_signature`; replay the first valid array and retain only pi's legacy encrypted tool-call fallback. Replace stale previous-pin tests. |
| F005 | blocking | runtime-impersonation | Usage counters are a JavaScript numeric-runtime emulator | `types.ts:382-403` | `types.rs:969-1158,2537-2581` | Pi counters are `number`. Rust accepts strings, booleans, arrays, objects, and null and implements JavaScript coercion/truthiness/string concatenation as public usage behavior. | Replace `UsageValue` with an IEEE-754-compatible numeric Rust type. Normalize or reject malformed provider fields at adapter boundaries; remove the general JavaScript coercion engine. |
| F006 | blocking | behavior-delta | Session registration lacks disposer and callback-identity deduplication | `session-resources.ts:1-10` | `session_resources.rs:3-29` | Pi uses Set identity and returns an unregister closure. Rust assigns a new ID on every registration and requires a separate unregister call, so the same `Arc` can run twice. | Preserve insertion order and `Arc` pointer identity, deduplicate identical callbacks, and return a one-shot disposer/registration handle that removes that callback. |
| F007, F141 | major | error-path-delta | Session cleanup loses live-Set traversal, aggregate errors, and panic isolation | `session-resources.ts:12-24` | `session_resources.rs:7-67` | Pi iterates the live Set, catches every thrown value, continues, and throws `AggregateError(..., "Failed to cleanup session resources")`. Rust snapshots callbacks, keeps only strings, and a panic prevents later cleanup. | Iterate an ordered live registry while releasing the lock around callbacks; honor additions/removals; retain boxed source errors; isolate panics; attempt all callbacks; return a source-bearing aggregate error with pi's message. |
| F008 | blocking | missing-feature | `KnownApi` and `KnownProvider` exports are absent | `types.ts:17-80`; `index.ts:37-40` | `types.rs:14-55`; `lib.rs:56` | Rust has only open string newtypes, so consumers cannot name or exhaustively match the known sets pi exports separately. | Add public enums for the current pin, truthful string serde/display, conversions into open `Api`/`ProviderId`, and root re-exports. |
| F009 | blocking | missing-feature | Public generic API/model/tool relationships are erased | `types.ts:243-280,324-342,449-519,821-850` | `types.rs:1266-1383,1993-2009,2144-2153`; `api/mod.rs:50-120` | Rust's Tool, ToolResultMessage, Model, StreamFunction, and options are erased/dynamic, allowing API/option mismatches and losing typed schema/detail relations. | Add an `ApiSpec` trait with associated options/compat types and public generic Tool/ToolResult/Model/StreamFunction types with dynamic defaults. Keep erasure only at internal dispatch seams. |
| F010 | blocking | missing-feature | `ApiStreamOptions` and `ProviderStreams` are not root re-exported | `types.ts:243-280`; `index.ts:37-40` | `api/mod.rs:50-120`; `lib.rs:14-64` | Equivalent root imports fail and require knowledge of an internal module relocation. | Add `pub use api::{ApiStreamOptions, ProviderStreams}` at the crate root. |
| F011 | blocking | behavior-delta | `ProviderStreams` changes optional arguments and cancellation errors | `types.ts:272-280` | `api/mod.rs:71-139` | Pi options are optional and cancellation rejects on failure. Rust requires options and returns an optional future whose error is an `AssistantMessage`. | Accept optional/defaultable options for every operation, distinguish unsupported capability from call failure, and return a typed cancellation error. |
| F012 | blocking | persisted-data-delta | Core content unions admit shapes pi excludes and normalize malformed persisted data | `types.ts:350-380,421-467` | `types.rs:727-879,1208-1214,1266-1294` | Rust permits arbitrary tool arguments, `Unknown`/`Null` content, defaulted assistant arrays, and null normalization beyond pi's declared unions. | Use map-valued tool arguments, exhaustive tagged content enums, and required vectors without manufacturing serde defaults. Confine provider extensions to adapter DTOs. |
| F013 | blocking | behavior-delta | Public pi `number` fields are narrowed to integer Rust types | `types.ts:118-120,179-232,409-425,449-465,535-545,803-850` | `types.rs:316-320,534-577,873-879,1189-1235,1266-1285,1437-1451,1993-2009`; `event_stream.rs:26-81` | Negative, fractional, and out-of-range values accepted by pi cannot be represented, while large Rust integers can serialize differently from a JS number. | Represent external numbers with binary64 or a dedicated JS-number newtype; perform checked integer/index conversion only at internal use sites. |
| F014 | minor | persisted-data-delta | Reloaded JSON loses JavaScript property ordering | `types.ts:112-120,421-467,521-525` | `types.rs:305-320,873-879,1208-1306` | Derived struct order, BTreeMap sorting, and plain insertion order do not reproduce integer-index keys first followed by string insertion order. | Preserve input field order where persisted bytes are observable and serialize maps with ECMAScript `OwnPropertyKeys` ordering. |
| F015 | minor | persisted-data-delta | Claimed JSON.stringify number formatting uses wrong exponent thresholds | `types.ts:100-105,163-177,382-403,722-818` | `types.rs:110-152,2806-2844` | Serde uses scientific notation at values such as `1e-6` and `1e19` where `JSON.stringify` uses fixed notation. | Add one ECMAScript shortest-number formatter (for example ryu-js) and boundary tests at `1e-6`, `1e-7`, `1e20`, and `1e21`. |
| F016, F055, F070, F130, F136 | blocking | runtime-impersonation | OpenAI paths bypass openai-oxide, spawn Node, and fabricate JS SDK identity | `utils/pi-user-agent.ts:1-19`; `openai-completions.ts:1-13,316-335,704-746`; `openai-responses.ts:1-2,140-159,218-259` | `utils/pi_user_agent.rs:48-153`; `openai_completions.rs:35,578-586,762-776,4975-4977`; `openai_responses.rs:15,334-374,574-589`; `openai_sse.rs:187-410`; `Cargo.toml:42-44` | Rust declares but does not use openai-oxide for these requests, hand-rolls HTTP/SSE, executes `node --version`, and sends false JS/Node/OpenAI-JS Stainless headers. | Route Completions and Responses through openai-oxide (extend/fork narrowly if hooks need it), delete all Node process execution and false headers, preserve pi's user-agent and observable request/retry/hook behavior, and emit only truthful native identity. |
| F017 | blocking | missing-feature | Tool schema origin is erased, losing pi's two validation modes | `utils/typebox-helpers.ts:14-23`; `utils/validation.ts:317-335` | `utils/typebox_helpers.rs:5-30`; `utils/validation.rs:269-279`; `types.rs:1375-1383` | Pi distinguishes native TypeBox schemas (`Value.Convert`) from serialized plain schemas (AJV-like coercion). Rust stores both as `Value` and always applies one coercer. | Introduce `ToolSchema::{Native,Serialized}` with identical wire JSON; builders create Native and deserialization creates Serialized. Port TypeBox conversion for Native and keep plain-schema coercion for Serialized. |
| F018 | major | behavior-delta | Streaming JSON drops partial `NaN` and infinities | `utils/json-parse.ts:1,104-120`; `package.json:71` | `utils/json_parse.rs:97-151,258-278` | Pinned partial-json accepts complete/partial `NaN`, `Infinity`, and `-Infinity`; serde `Value` cannot represent them and Rust drops the field. | Use a lossless streaming dynamic value including all three non-finite atoms, port partial-json 0.1.7 branches, and convert explicitly to strict JSON only at strict provider boundaries. |
| F019 | major | behavior-delta | Context estimates use serde JSON spelling/order, changing token estimates | `utils/estimate.ts:21-27,45-60,105-107` | `utils/estimate.rs:69-86,135-143` | Pi measures UTF-16 length of `JSON.stringify`; Rust measures `serde_json::to_string`, so exponent spelling and key order change token counts and output clamping. | Use the shared ECMAScript JSON formatter for arguments/tools and add number-threshold, negative-zero, nested-schema, and numeric-key vectors. |
| F020 | major | error-path-delta | Provider error bodies use Rust trimming instead of JavaScript trimming | `utils/error-body.ts:76-82` | `utils/error_body.rs:7-29,58-70` | Rust defines the correct helper but calls `str::trim`, changing U+FEFF/U+0085 behavior, body text, truncation, and formatting. | Call `trim_javascript_whitespace` and add U+FEFF/U+0085 boundary tests. |
| F021 | major | error-path-delta | Error-body UTF-16 cap cannot retain a split surrogate | `utils/error-body.ts:137-139` | `utils/error_body.rs:128-149` | JS `slice` can retain one surrogate unit; Rust skips the whole scalar, changing the capped body and suffix boundary. | Use the lossless UTF-16/WTF-8 string type at this boundary, slice exact code units, and define truthful display/JSON escaping plus astral boundary tests. |
| F022 | major | behavior-delta | Integral f64 values can be coerced to strings in integer unions | `utils/validation.ts:30-47,211-220` | `utils/validation.rs:20-30,152-161` | Pi's `Number.isInteger(1.0)` matches; Rust tests only serde integer storage and may take a later string arm. | Match any finite number with zero fractional part as integer and test every union order with f64-backed integral values. |
| F023 | blocking | behavior-delta | Numeric coercion rejects finite radix values wider than u128 | `utils/validation.ts:59-90` | `utils/validation.rs:33-91` | JavaScript `Number` accepts arbitrarily long radix strings while Rust's u128 parser fails before binary64 rounding. | Parse radix strings with arbitrary precision, round to IEEE-754 binary64, and apply pi's finite check; test 2^53, u128, max-finite, and overflow boundaries. |
| F024 | major | error-path-delta | Tool validation errors expose jsonschema wording rather than TypeBox messages | `utils/validation.ts:282-293,341-349` | `utils/validation.rs:281-314` | User-visible wording, grouping, ordering, and paths differ from TypeBox 1.3.7 en_US. | Adapt `ValidationErrorKind` into exact TypeBox-compatible messages and ordering while retaining Rust sources; snapshot every supported keyword and multi-error case. |
| F025, F047 | blocking | behavior-delta | Retry-After date parsing is narrower than JavaScript `Date.parse` | `utils/provider-retry.ts:51-66`; `auth/oauth/github-copilot.ts:154-164` | `utils/provider_retry.rs:146-175,280-323`; `auth/oauth/github_copilot.rs:209-229` | Slash dates and ISO forms accepted by pi may parse as zero/no-delay in Rust, changing retries and cap errors. | Implement a subprocess-free parser matching the pinned runtime's relevant Date.parse grammar and share it with Copilot; pin numeric, HTTP, ISO, slash, month-name, local, past, and invalid vectors. |
| F026 | major | behavior-delta | Overflow patterns treat Unicode digits as token counts | `utils/overflow.ts:37-63` | `utils/overflow.rs:7-42` | JavaScript `\d` here is ASCII; Rust regex `\d` is Unicode-aware. | Replace atoms with `[0-9]` or disable Unicode and test ASCII/non-ASCII digits for every numeric pattern. |
| F027 | blocking | behavior-delta | Diagnostics reject thrown values and discard codes | `utils/diagnostics.ts:15-30` | `utils/diagnostics.rs:13-35` | Pi accepts any thrown value, preserves string/number code, and falls back from an empty Error message to its name. Rust requires `Error`, hard-codes no code, and cannot accept `Err(String)`. | Add an idiomatic diagnostic input enum/adapter for typed errors and displayable thrown values with optional truthful name, code, and backtrace; never synthesize JavaScript identity. |
| F028 | major | error-path-delta | Invalid proxy URL errors leak url-crate wording | `utils/node-http-proxy.ts:92-109` | `utils/node_http_proxy.rs:113-136` | Pi exposes stable `Invalid proxy URL ...: Invalid URL`; Rust exposes parser-specific text such as `invalid IPv6 address`. | Add a structured invalid-URL error with stable Display and retain `url::ParseError` as source; test malformed IPv6/domain/percent/port cases. |
| F029 | minor | behavior-delta | Provider error JSON has different number spelling and numeric-key ordering | `utils/error-body.ts:84-90,142-148` | `utils/error_body.rs:89-109,151-153,217-241` | Parsed objects with numeric keys or threshold numbers stringify differently. | Use the shared ECMAScript JSON formatter recursively. |
| F030 | minor | error-path-delta | Very large retry-cap errors format seconds differently | `utils/provider-retry.ts:37-48` | `utils/provider_retry.rs:177-201` | Pi uses JS Number-to-string; Rust forces fixed decimal text. | Format the ceiled seconds with the ECMAScript number formatter and add exponent-threshold snapshots. |
| F031 | blocking | behavior-delta | Embedded model catalog does not match pi-ai 0.84.2 | `models.generated.ts:4-124`; published `dist/providers/data/.manifest.json` and shards | `models_generated.rs:6-93`; `providers/data/.manifest.json`; provider shards | Rust embeds a later/different manifest: 1,294 versus 1,267 flattened models, with 43 Rust-only, 16 missing, and 32 metadata-changed shared IDs. | Replace manifest/shards with exact published 0.84.2 artifacts, regenerate `MODELS`, and assert pinned hashes and timestamp rather than live data. |
| F033 | blocking | readme-not-recreatable | Root TypeBox-style schema builder and quickstart are absent | `index.ts:1-2`; `README.md:98-119` | `lib.rs:32-64`; `utils/typebox_helpers.rs:5-30` | Pi re-exports `Type`, `Static`, and `TSchema`; Rust offers only `string_enum`, forcing raw JSON in the first README tool example. | Root-reexport a typed JSON-Schema builder covering Object/String/Optional/Array/options and pair it with Rust schema/static traits; port the quickstart without raw-schema workaround. |
| F034 | blocking | missing-feature | `ModelsError` and `ModelsErrorCode` are not root/compat exports | `models.ts:37`; `index.ts:34`; `compat.ts:27` | `models.rs:4-6`; `lib.rs:46-64`; `compat/mod.rs:18-43` | Public pi names exist only through an internal Rust path. | Publicly re-export from models, crate root, and compat. |
| F035, F138 | blocking | non-idiomatic | User-controlled compat dispatch failures panic | `compat.ts:102-123,242-288` | `compat/mod.rs:82-105,364-439` | API mismatches and absent registrations are recoverable pi errors but Rust `panic!` can unwind/abort the host. | Add source-bearing `CompatError`; return `Result` for synchronous checked dispatch and complete helpers, or terminal error streams where the trait cannot change; remove panic paths. |
| F036, F127 | blocking | runtime-impersonation | Native CLI advertises the JavaScript `npx` package | `cli.ts:79-86`; `README.md:1541-1549` | `bin/pi-ai.rs:170-178`; `Cargo.toml:1-15` | The compiled Rust executable prints `Usage: npx @earendil-works/pi-ai`, directing users to a different runtime/package. | Print the actual native executable (`pi-ai` or argv[0]) and document Cargo/native installation while retaining command behavior. |
| F037, F140 | major | error-path-delta | Pluggable model storage failures are forced into aborts and lose causes | `models-store.ts:20-25`; `models.ts:73-76,338-429`; `auth/resolve.ts:26-41` | `models_store.rs:22-38`; `models.rs:57-74,744-840`; `auth/resolve.rs:37-61` | A file/database store cannot return its real I/O error; Rust rewrites it as `AbortReason`/new text and error wrappers expose no source. | Add object-safe `ModelsStoreError::{Aborted,Storage}` with owned sources; preserve source errors unchanged through refresh/publication and implement `Error::source` transparently on model error wrappers. |
| F038 | major | behavior-delta | Concurrent ready faux deferred polls collapse distinct results | `providers/faux.ts:602-622` | `providers/faux.rs:700-770` | Pi lets each contender that saw no final resolve and stream its own result; Rust makes all contenders stream the first committed value. | Resolve locally after observing absence, unconditionally assign that result to the entry, and stream the same local result; add a stateful concurrent-poll test. |
| F039, F123 | major | doc-misleading | Model catalog/store source anchors cite nonexistent pi ranges | `model-catalog.ts:22-27`; `models-store.ts:27-45` | `model_catalog.rs:75-76`; `models_store.rs:111-112` | Comments cite past EOF or miss the asserted implementation. | Correct to `model-catalog.ts:22-26` and `models-store.ts:27-44`, or cite symbols. |
| F040 | minor | non-idiomatic | `BuiltinProvider` is an unconstrained string alias | `providers/all.ts:50-58` | `providers/all.rs:27-45` | Pi's generated provider key set is closed; any Rust string literal satisfies the alias. | Generate a non-exhaustive enum with Display/FromStr/iteration, use it in typed accessors, and retain a fallible dynamic string lookup. |
| F041 | minor | non-idiomatic | Read-only `Models` and mutable `MutableModels` are collapsed | `models.ts:156-230` | `models.rs:575-580,603-650` | Rust cannot express or enforce the read-only collection capability because all mutations live on `Models`. | Provide distinct read-only/mutable traits or views over shared state; `create_models` returns mutable, which can be borrowed/coerced as read-only. |
| F042 | blocking | runtime-impersonation | Copilot traffic falsely identifies VS Code and Copilot Chat | `auth/oauth/github-copilot.ts:13-19,181-186` | `auth/oauth/github_copilot.rs:47-62,304-312,524-531,609-615` | Rust copies VS Code/extension/integration headers although the crate is none of those. | Add truthful configurable host/client identity and an authorized native OAuth integration; emit VS Code identities only when a host explicitly and truthfully supplies them. |
| F043 | blocking | missing-feature | Built-in OAuth flows do not expose replaceable HTTP transport | OAuth operations in `auth/oauth/{anthropic,github-copilot,kimi-coding,openai-codex,openrouter,radius,xai}.ts` | corresponding Rust modules; public factories hardwire `default_fetch()` (for example `anthropic.rs:475-533`, `openai_codex.rs:798-868`) | Pi's ambient fetch is replaceable at operation time, enabling its tests, proxying, tracing, and policy hooks. Rust transport-injected constructors are private test helpers. | Add a public OAuth runtime/config with `Arc<dyn FetchFunction>` and route every authorize/exchange/refresh/device/Copilot request through it, retaining default factories. |
| F044 | major | behavior-delta | OpenRouter may exchange callback and manual codes in one login | `auth/oauth/openrouter.ts:169-205,234-237,269-285` | `auth/oauth/openrouter.rs:272-314,465-489,862-914` | Once pi's callback is claimed, manual completion is ignored. Rust can start a second exchange while the claimed callback exchange is pending. | Separate cancellation from result selection; once claimed, await only callback result. Test a delayed callback exchange racing manual completion and assert one token request. |
| F045 | major | error-path-delta | Anthropic rejects malformed successful token JSON pi accepts | `auth/oauth/anthropic.ts:217-231,333-352` | `auth/oauth/anthropic.rs:305-361,442-472` | Pi casts successful JSON and can construct undefined token fields/NaN expiry; Rust validates exact token strings and numeric expiry. | Decode a permissive raw response and reproduce pi's construction/coercion behavior before use; test malformed successful exchange and refresh payloads. |
| F046 | major | error-path-delta | Radius over-validates successful token and device-code payloads | `auth/oauth/radius.ts:126-139,291-302` | `auth/oauth/radius.rs:204-240,480-511` | Pi token success has no field validation and device code uses JS truthiness/coercion; Rust requires exact strings/numbers and rejects zero. | Use a raw JSON intermediate and reproduce truthiness/numeric coercion for absent, wrong-type, zero, string-number, and non-finite cases. |
| F048 | major | error-path-delta | OAuth loopback servers can hang on stalled sockets/listener failure | `auth/oauth/anthropic.ts:113-151`; `openai-codex.ts:335-391`; `radius.ts:174-216` | `auth/oauth/mod.rs:189-214`; flow modules `anthropic.rs:236-255`, `openai_codex.rs:655-670`, `radius.rs:353-374` | Rust reads one accepted connection inline without timeout, blocking the real callback; some listener failures break without settling the waiter. | Process sockets concurrently, cap header size/time, and settle pending callbacks on terminal listener failure. Test stalled sockets and forced listener errors for all loopback flows. |
| F049 | major | behavior-delta | Radius observes pre-aborted signals that pi misses | `auth/oauth/radius.ts:147-167,239-254` | `auth/oauth/radius.rs:333-369,404-428` | Pi installs a listener without checking `aborted`; Rust's cancellation future is immediately ready. | Model this flow's listener-after-registration semantics and test a signal aborted before listener installation. |
| F050 | major | error-path-delta | Radius refresh loses status and OAuth error metadata | `auth/oauth/radius.ts:68-81,102-124,386-397` | `auth/oauth/radius.rs:45-69,647-663` | Pi's downcastable error exposes status and oauthError; Rust discards them into text. | Preserve status and OAuth code in typed common auth error metadata or a retained source while keeping message parity. |
| F051 | minor | persisted-data-delta | OAuth expiry retains sub-millisecond precision | OAuth credential construction such as `kimi-coding.ts:120-139`, `openai-codex.ts:126-146`; `auth/resolve.ts:135-136` | `auth/oauth/mod.rs:150-155`; `auth/resolve.rs:233-239` | Pi `Date.now()` is integer milliseconds; Rust retains floating sub-millisecond fractions. | Floor/use `as_millis()` everywhere emulating Date.now. |
| F052 | minor | persisted-data-delta | Credential JSON changes key order and integer spelling | OAuth credential construction in `kimi-coding.ts:267-279`, `openai-codex.ts:403-415`, `openrouter.ts:127-132`, `radius.ts:133-139`, `xai.ts:137-142` | `auth/types.rs:81-90` | Pi creates type/access/refresh/expires in insertion order; Rust declares refresh before access and uses serde float spelling. | Implement a credential serializer preserving pi field order and ECMAScript number formatting. |
| F053 | minor | non-idiomatic | Environment detection blocks inside async future | `auth/context.ts:23-43` | `auth/context.rs:23-32` | Rust calls synchronous filesystem metadata from async code. | Use `tokio::fs::metadata` or `spawn_blocking` while preserving tilde and existence semantics. |
| F057 | blocking | missing-feature | Public OpenAI message conversion cannot take resolved compat/options | `openai-completions.ts:1131-1136,1283-1293`; direct tests in `deferred-tools.test.ts:357-395`, `openai-completions-thinking-as-text.test.ts:117-148` | `openai_completions.rs:103-106,1994-2012` | Rust recomputes private compat and ignores its public conversion-options type, so direct pi tests require internal workarounds. | Expose public resolved compat and a conversion function accepting it plus `ConvertCompletionsMessagesOptions`; keep the two-argument auto-detect wrapper. |
| F058 | blocking | wire-delta | Explicit Authorization tombstone is ignored | `openai-completions.ts:67-80,713-746` | `openai_completions.rs:762-816` | Rust removes the header, then unconditionally regenerates `Bearer api_key`, transmitting a credential explicitly omitted by the caller. | Build authentication defaults first, apply case-insensitive model/options tombstones last, and never regenerate explicitly removed headers. |
| F059 | major | error-path-delta | Completions `stream_simple` defers missing-key failure into the stream | `openai-completions.ts:76-80,683-701` | `openai_completions.rs:423-447,530-534,5537-5554` | Pi throws at entry; Rust returns a stream and later emits Error. A Rust test comment asserts the stale opposite. | Return `Result<AssistantMessageEventStream, SetupError>` and resolve the key before stream construction; retain in-band post-setup failures. |
| F060, F082, F096, F105 | blocking | behavior-delta | Shared `onPayload` contract drops in-place mutations | `types.ts:142-145`; provider calls in `openai-completions.ts:317-321`, `anthropic-messages.ts:565-576`, Google adapters, and `bedrock-converse-stream.ts:263-267` | `types.rs:454-459`; corresponding Rust provider call sites | Rust passes a cloned Value and discards mutation on `None`; pi mutates the live payload and optionally replaces it. Anthropic Rust also permits replacement `stream:false`, while pi forces streaming afterward. | Redesign hook as async mutation over `&mut` typed payload plus optional replacement; retain mutation on None. Apply across all providers and enforce provider post-hook invariants such as Anthropic `stream:true`. |
| F061 | major | error-path-delta | OpenAI timeout validation and custom-fetch abort signal differ | `openai-completions.ts:322-329,740-746` plus OpenAI 6.40 validator | `openai_completions.rs:555,578-586`; `openai_sse.rs:218-283` | Rust accepts fractional/negative/non-finite values and turns some into zero timeout; injected fetch receives only caller abort rather than caller+timeout composition. | Validate integer/nonnegative timeout with pinned SDK messages, compose caller and timeout abort signals, pass the composed signal to all transports, and default only when absent. |
| F062 | blocking | wire-delta | OpenAI SDK environment headers are absent | `openai-completions.ts:704-746` plus OpenAI 6.40 client | `openai_completions.rs:762-816` | Pi inherits organization, project, and custom environment headers with SDK precedence; Rust's manual builder omits them. | Resolve via pluggable ProviderEnv/process fallback and reproduce precedence/tombstones, preferably through openai-oxide. |
| F063 | major | error-path-delta | OpenAI transport/body failures collapse to `Connection error.` | `openai-completions.ts:327-334,509-633,657-675` plus OpenAI 6.40 client | `openai_sse.rs:218-249,310-330,394-399` | Rust discards injected/native causes, non-2xx body-read status, and successful stream-body errors, losing timeout classification and messages. | Use a structured transport error retaining cause, abort/timeout class, status, headers, and body-read failure; feed it through retry and provider formatting. |
| F064 | major | error-path-delta | Manual OpenAI SSE decoding changes thread events and malformed-JSON diagnostics | `openai-completions.ts:1-13,509-510,657-675` plus OpenAI 6.40 streaming implementation | `openai_sse.rs:340-486` | Rust discards event names so `thread.*` data can be processed as chunks and emits serde diagnostics without equivalent raw-frame logging. | Delegate to openai-oxide or reproduce SDK wrapping, raw-frame diagnostics, native malformed JSON behavior, `[DONE]`, and streamed API errors. |
| F065 | major | behavior-delta | Valid JSON chunks with malformed field types are silently defaulted | `openai-completions.ts:509-538,590-617,1503-1526` | `openai_completions.rs:865-925,1031-1069` | Pi's raw operations retain/coerce some truthy values and throw on others; Rust optional-string/vector deserializers drop them to None/empty. | Retain raw Values, implement pi's explicit truthiness/type operations, and error wherever pi's operation errors before constructing typed state. |
| F066 | major | wire-delta | OpenAI base URL joining/canonicalization differs | `openai-completions.ts:327-329,740-746` plus OpenAI 6.40 client | `openai_completions.rs:578-585` | Rust removes all trailing slashes and passes an unparsed URL; SDK removes at most one boundary slash and canonicalizes. | Build with a URL type, reproduce the one-boundary-slash rule, and pass the canonicalized URL to native/injected transports. |
| F067, F091 | major | behavior-delta | Provider response hooks lose Fetch Headers normalization/repeated values | `utils/headers.ts:3-8`; OpenAI `openai-completions.ts:335`; Anthropic `anthropic-messages.ts:583` | `openai_sse.rs:197-215,298-307`; `anthropic_messages.rs:1430-1491`; `types.rs:316-369,460-465` | Rust overwrites repeated native headers and may expose injected casing; pi exposes lowercase names with combined repeated values. | Carry multi-value ordered headers through transport and normalize/fold once with Fetch-compatible semantics for all hook responses. |
| F069 | minor | wire-delta | OpenAI request/nested key order differs | `openai-completions.ts:763-814,972-987,1215-1308` | `openai_completions.rs:235-255,370-399,1577-1583,1896-1908` | Flattened maps, BTreeMap, and fixed struct fields reorder `tool_stream`, chat-template values, and reasoning/tool calls. | Assemble final request/messages in insertion-ordered maps following pi's mutation sequence and preserve caller object order. |
| F072 | major | error-path-delta | Responses/Codex `stream_simple` do not fail missing credentials at call time | `openai-responses.ts:198-215`; `openai-codex-responses.ts:492-512` | `openai_responses.rs:206-234`; `openai_codex_responses.rs:312-333` | Rust returns a stream and can report runtime absence before credential absence; pi validates before returning. | Return `Result<AssistantMessageEventStream, SetupError>` from simple entry points/dispatch and validate before runtime acquisition. |
| F073 | blocking | missing-feature | Codex WebSockets ignore ProviderEnv proxy configuration | `openai-codex-responses.ts:957-983,1471-1479` | `openai_codex_responses.rs:712-723`; `openai_codex_responses/transport.rs:356-383` | Pi resolves target-specific proxy and constructs proxied sockets where needed; Rust always connects directly. | Thread ProviderEnv through acquisition and implement Rust-native CONNECT/TLS proxying with matching target/no-proxy behavior. |
| F074 | major | error-path-delta | Codex does not retry rejected `onResponse` hooks | `openai-codex-responses.ts:382-449` | `openai_codex_responses.rs:1422-1503` | Rust returns directly on hook failure, bypassing maxRetries; pi routes it through the attempt catch/retry guards. | Convert hook failure into the current attempt error and run the common abort/delay/usage-limit retry logic. |
| F075 | major | error-path-delta | Empty Codex error messages suppress provider error code | `openai-codex-responses.ts:704-730` | `openai_codex_responses/transport.rs:846-880` | Pi falls through empty message to code; Rust treats `Some("")` as selected. | Treat empty message/code as absent before message → code → serialized-event fallback. |
| F076 | major | behavior-delta | Responses `responseId` assignment differs for absent/empty IDs | `openai-responses-shared.ts:556-558,597-600` | `openai_responses_shared.rs:1509-1511,1629-1632` | Created events in pi may clear prior state with absence; terminal updates require truthy ID. Rust does the reverse in these edge cases. | Assign decoded Option directly on created; update terminal ID only when nonempty. |
| F077 | major | error-path-delta | Terminal Responses `output:null` fails instead of finalizing | `openai-responses-shared.ts:533-555` | `openai_responses_shared.rs:987-1007,1137-1139` | Pi treats null/absent output as `[]`; Rust's `Vec` deserialization rejects explicit null. | Deserialize null as empty using Option/custom deserializer and add terminal coverage. |
| F078 | major | wire-delta | Codex WebSocket idle timeout sends close reason `done` | `openai-codex-responses.ts:1354-1365` | `openai_codex_responses/transport.rs:535-545,705-730` | Pi closes code 1000 reason `idle_timeout`; Rust later performs generic cleanup with reason `done`. | Close immediately with `idle_timeout` on timer and prevent cleanup from replacing the reason. |
| F080 | blocking | missing-feature | Injected Anthropic clients cannot execute their message API | `anthropic-messages.ts:267-271,528-576`; `anthropic-sse-parsing.test.ts:33-78` | `anthropic_messages.rs:114-140,1040-1092,1286-1300,2641-2646` | Pi invokes `options.client.messages.create().asResponse()`, supporting alternative clients. Rust extracts metadata/key then reconstructs a separate request, ignoring client transport/middleware/behavior. | Define a public async executable message-client abstraction, implement it for adk-anthropic and hosts, and pass payload/signal/timeout/retry to the client. |
| F081 | blocking | non-idiomatic | Default Anthropic requests bypass mandated adk-anthropic | `anthropic-messages.ts:575-583`; `package.json:62-64` | `anthropic_messages.rs:136-140,1407-1522` | The adk client is reduced to an API-key holder while a hand-built reqwest path sends every request. | Route default create/stream through adk-anthropic; extend/wrap its typed request for pi-only fields without JS SDK impersonation. |
| F083 | blocking | wire-delta | Custom adaptive-thinking effort strings are discarded | `anthropic-messages.ts:805-822` | `types.rs:175-220`; `anthropic_messages.rs:926-949` | Pi forwards any mapped string; Rust recognizes only a closed set and falls back (`"ultra"` becomes `"high"`). | Use an open effort string/newtype and preserve present mapped values verbatim; fallback only when absent/null. |
| F084 | major | error-path-delta | Anthropic direct `stream_simple` missing-auth control flow differs | `anthropic-messages.ts:825-833` | `anthropic_messages.rs:874-886` | Pi fails before stream construction; Rust returns an already-terminal error stream. | Return `Result<AssistantMessageEventStream, AnthropicError>` for direct preflight and keep provider-layer catch/in-band conversion separate. |
| F085 | blocking | wire-delta | Copilot Anthropic model headers override dynamic headers in wrong order | `anthropic-messages.ts:539-560,905-918`; `github-copilot-headers.ts:23-36` | `anthropic_messages.rs:1227-1234,1273-1282` | Rust applies model headers after context-derived dynamic headers; pi applies dynamic headers after model headers and explicit request headers last. | Merge fixed → model → dynamic → request headers with case-insensitive replacement; test conflicts. |
| F086 | major | wire-delta | Null Anthropic tool-schema properties/required are transmitted | `anthropic-messages.ts:1337-1351` | `anthropic_messages.rs:626-641` | Pi defaults missing or null to `{}`/`[]`; Rust defaults only missing and sends null. | Treat `Value::Null` as absent for both fields in strict and legacy schema paths. |
| F087 | major | behavior-delta | Anthropic OAuth tool-name restoration is ASCII-only | `anthropic-messages.ts:102-112` | `anthropic_messages.rs:340-353` | JavaScript lowercase handles Unicode; `eq_ignore_ascii_case` does not, potentially breaking dispatch. | Compare Unicode lowercase with JS-compatible semantics and return the original declared name. |
| F088 | major | behavior-delta | Initial UTF-8 BOM can hide first Anthropic SSE event | `anthropic-messages.ts:401-428` | `anthropic_messages.rs:1556-1635` | TextDecoder consumes one initial BOM; Rust leaves U+FEFF in the field name so `message_start` can be ignored. | Strip exactly one decoder-initial BOM, including split-chunk cases. |
| F089 | major | error-path-delta | Malformed recognized Anthropic SSE events are silently accepted | `anthropic-messages.ts:589-728` | `anthropic_messages.rs:1761-2089` | Pi's required property access throws; Rust often returns Ok or substitutes empty/zero values, allowing false success. | Use typed event DTOs or explicit validation for every field pi requires, preserving only explicit nullish defaults; add malformed-event terminal tests. |
| F090 | major | error-path-delta | Anthropic HTTP/transport errors do not match SDK surface | `anthropic-messages.ts:575-583,784-792`; pinned SDK 0.91.1 | `anthropic_messages.rs:1360-1520` | Rust drops status, emits empty text for empty bodies, and leaks reqwest strings instead of pinned SDK connection/status messages. | Normalize status/body/transport/timeout to the SDK-observable messages while retaining structured status/headers for retries. |
| F092 | minor | wire-delta | Anthropic integral and negative-zero temperatures serialize differently | `anthropic-messages.ts:1035-1039,575-576` | `anthropic_messages.rs:762-769,1084-1089` | Pi writes `1`/`0`; serde writes `1.0`/`-0.0`. | Normalize request numbers through the ECMAScript formatter and add byte-level payload tests. |
| F093 | major | doc-misleading | Anthropic parity comments cite wrong source lines | `providers/anthropic.ts:18-38` | `providers/anthropic.rs:153-154,200-202` | Cited ranges do not contain the stored-credential/fallback behavior claimed. | Anchor to `providers/anthropic.ts:18-38`, specifically fallback lines 33-37. |
| F095 | blocking | wire-delta | Vertex Gemini 2.5 Pro high reasoning uses Flash budget | `google-vertex.ts:568-595` | `google_vertex.rs:337-358` | Pi sends 32768 for Pro high; Rust combines Pro/Flash and sends 24576. | Split tables exactly and test all Pro levels. |
| F097 | blocking | runtime-impersonation | Google payload hooks receive fake abort JSON, not live signal | `google-generative-ai.ts:88-93,403-408`; `google-vertex.ts:106-111,501-506` | Google Rust adapters and `google_generative_ai/tests.rs:744-752` | Rust inserts `{aborted:false}`, drops it before wire, and always uses original options signal; it can become stale and replacement cannot control cancellation. | Use a typed payload carrying an opaque live `Arc<dyn AbortSignal>` beside serializable config; expose/use the post-hook handle and never serialize an AbortSignal-shaped object. |
| F098 | blocking | error-path-delta | Google/Vertex simple setup failures become error events | `google-generative-ai.ts:297-316`; `google-vertex.ts:314-331` | `google_generative_ai.rs:1744-1777`; `google_vertex.rs:289-315` | Missing key and invalid thinking-map errors occur before stream return in pi but in-band after stream creation in Rust. | Return `Result<AssistantMessageEventStream, GoogleSetupError>` and validate before stream creation/dispatch. |
| F099 | major | error-path-delta | Google HTTP failures expose adk wrapper text | `google-generative-ai.ts:280-289`; `google-vertex.ts:297-306`; `utils/error-body.ts:38-53,128-134` | `google_generative_ai.rs:139-151,774-783`; `google_shared.rs:553-558` | Pi normalizes the SDK ApiError/parsed body; Rust delegates to adk's `bad response from server...` Display and raw JSON whitespace. | Carry status plus normalized parsed/compact body message and use it for retry/final errors rather than adk Display. |
| F100 | blocking | missing-feature | Vertex rejects credential-file formats accepted by GoogleAuth | `google-vertex.ts:353-368,420-423`; `providers/google-vertex.ts:49-62` | `google_vertex.rs:230-263` | Rust assumes UTF-8 JSON and whitelists four types, excluding other pinned GoogleAuth keyFilename formats. | Use/implement a Rust credential loader matching GoogleAuth formats, including external-account authorized-user and supported service-account key files, behind host-pluggable filesystem access. |
| F101 | minor | error-path-delta | Explicit null thinking mapping is reported as undefined | `google-shared.ts:38-49`; `types.ts:85` | `google_shared.rs:53-85`; `types.rs:175-219` | Rust collapses present-null and absent; pi diagnostic distinguishes `null` from `undefined`. | Preserve absent/null/string tri-state and format accordingly. |
| F106 | blocking | behavior-delta | Bedrock hook sees lossy JSON instead of typed command input | `bedrock-converse-stream.ts:251-265,989-1027,1266-1287`; `bedrock-redacted-reasoning.test.ts:208-234` | `bedrock_converse_stream.rs:391-447,630-645,732-767`; `types.rs:454-459` | Rust removes unset fields and converts image/redacted bytes to base64 strings before the hook; pi exposes optional fields and byte arrays before SDK serialization. | Add a typed Bedrock command hook payload with Options and `Vec<u8>`/blob values; encode/omit only after the hook. Port byte and optional-field assertions. |
| F107 | blocking | wire-delta | Bedrock transmits hook-added unknown fields SDK would filter | `bedrock-converse-stream.ts:263-269` | `bedrock_converse_stream.rs:2037-2055` | Pi passes replacement through the modeled SDK command; Rust serializes arbitrary remaining JSON. | Filter/deserialize through a typed ConverseStream request and let aws-sdk-bedrockruntime serialize it; test wire capture with unknown and supported extension fields. |
| F108 | blocking | persisted-data-delta | Lone-surrogate Bedrock conversion cases are unrepresentable in current public strings | `bedrock-converse-stream.ts:866-898,989-1027`; `bedrock-convert-messages.test.ts:271-305` | `bedrock_converse_stream.rs:579-767`; `types.rs:644-692,853-879,1298-1305`; `bedrock_converse_stream/tests.rs:65-139` | Pi ingests lone UTF-16 surrogates and removes them before Bedrock conversion; Rust `String`/serde rejects the persisted input and omitted both tests. | Use the lossless UTF-16/WTF-8 text boundary with idiomatic constructors; sanitize at provider conversion and port both system/user/assistant/thinking cases. |
| F109 | major | error-path-delta | Bedrock modeled stream exceptions are flattened | `bedrock-converse-stream.ts:300-309,373-390`; `bedrock-error-metadata.test.ts:132-140` | `bedrock_converse_stream.rs:1857-1925`; `bedrock_converse_stream/tests.rs:1030-1082` | Pi normalizes a bare exception object to compact JSON such as `{"message":"failure"}`; Rust extracts only `failure`. | Construct/compactly stringify the modeled exception object and update the real-SDK stream test. |
| F110 | major | error-path-delta | Bedrock raw HTTP bodies are not trimmed/capped | `bedrock-converse-stream.ts:373-390`; `utils/error-body.ts:76-81,137-139` | `bedrock_converse_stream.rs:1116-1146,1812-1854` | Rust includes all raw UTF-8 bytes verbatim; pi trims JS whitespace and caps at 4,000 UTF-16 units before composing status/body and retention hints. | Route through shared provider-error normalization with exact trim/cap and add padded, long, and non-BMP bodies. |
| F111 | major | test-gap | Bedrock headers are not tested through public stream/SDK interceptor | `bedrock-custom-headers.test.ts:89-201`; `bedrock-response-headers.test.ts:37-61` | `bedrock_converse_stream/tests.rs:525-599` | Unit helpers do not pin interceptor registration, signing-stage injection, raw response capture, callback timing, or public propagation. | Add a local-server public-stream integration test covering allowed/reserved request headers, exact response status/headers, one callback, and callback-before-terminal ordering. |
| F112 | blocking | missing-feature | Cloudflare binding cannot reproduce Request/init composition or one-shot bodies | `cloudflare-gateway-binding.ts:73-90,127-190`; binding tests `83-98,128-177,258-286` | `cloudflare_gateway_binding.rs:50-54,93-104`; `types.rs:336-369` | Rust receives one flattened request with buffered bytes, lacking init override states and streamed/FormData/Blob body consumption. | Add request-body text/bytes/async-stream variants and binding-facing Request+init overrides for headers/body/signal; collect one-shot bodies once and port direct tests. |
| F113 | major | wire-delta | Cloudflare binding JSON parsing uses non-JavaScript number semantics | `cloudflare-gateway-binding.ts:127-141` | `cloudflare_gateway_binding.rs:93-121` | JS rounds 9007199254740993 and accepts 1e400 as Infinity; serde preserves u64/rejects overflow. | Parse into the binary64/non-finite dynamic value foundation with ECMAScript JSON.parse numeric semantics. |
| F114 | blocking | wire-delta | Bare trailing `?` changes Cloudflare binding endpoint | `cloudflare-gateway-binding.ts:118-125` | `cloudflare_gateway_binding.rs:78-92` | WHATWG `URL.search` is empty for a bare delimiter; Rust appends `?` for `Some("")`. | Append query only when nonempty and add bare/empty/nonempty query tests. |
| F115 | major | wire-delta | Binding header collisions resolve in sorted rather than caller order | `cloudflare-gateway-binding.ts:169-190` | `cloudflare_gateway_binding.rs:105-116`; `types.rs:336-343` | BTreeMap sorting before lowercase changes which case variant wins and enumeration order. | Carry ordered header pairs/IndexMap, lowercase/collapse in caller order, then remove forbidden headers. |
| F116 | blocking | behavior-delta | Cloudflare placeholder replacement ignores ECMAScript dollar substitutions | `providers/cloudflare-stream.ts:10-14` | `providers/cloudflare_stream.rs:14-23` | JS string replacement interprets `$&`, `$$`, `$\``, and `$'`; Rust inserts the environment value literally. | Implement the literal-search ECMAScript replacement-substitution helper and apply account then gateway replacements sequentially. |
| F117 | blocking | wire-delta | Radius config refresh sends explicit empty GET body | `providers/radius-config.ts:80-94` | `providers/radius_config.rs:132-163`; `auth/oauth/mod.rs:125-141` | Pi sends no body; Rust always stores `Some(Vec::new())`, potentially emitting Content-Length: 0. | Accept optional body in the shared helper and use None for this GET. |
| F118 | blocking | missing-feature | Public Radius model helpers return unusable intermediate records | `providers/radius-config.ts:61-73` | `providers/radius_config.rs:91-120`; `providers/radius.rs:47-63` | Pi returns `Model[]`; Rust returns `Vec<RadiusResolvedModel>` with the model nested and keeps Model conversion private. | After making Model lossless, return `Vec<Model>` publicly; keep any raw intermediate private or explicitly separate. |
| F119, F120, F131, F139 | blocking | persisted-data-delta | Radius publication drops accepted models and unknown properties | `providers/radius-config.ts:26-49,61-67`; `providers/radius.ts:67-77` | `providers/radius.rs:40-69,194-208,483-603`; `providers/radius_config.rs:10-24,91-110`; closed model types in `types.rs:1415-1452,1993-2105` | Pi shallow-validates then spreads every accepted model, preserving fractional/negative limits, unknown modalities/cost/map/top-level fields. Rust narrows through closed Model types and `filter_map`; tests deliberately require loss. | Make Model wire representation lossless/open with JS-number limits and flattened/raw extensions; remove filtering and publish/persist every shallow-valid entry byte-value-equivalently. Replace loss-oriented tests. |
| F122 | major | test-gap | Catalog tests overclaim provider metadata/model coverage | Numerous pi catalog tests including `baseten-models.test.ts:17-75`, `together-models.test.ts:16-78`, `qwen-token-plan-models.test.ts:113-215`, `zai-coding-plan-models.test.ts:4-53`, `fireworks-models.test.ts:21-149`, `supports-xhigh.test.ts:55-154`, `xai-responses.test.ts:115-134` | `models_generated.rs:160-654`; `env_api_keys.rs:130-187` | Rust checks subsets while comments claim the full cited cases, omitting exact compat, prices, maps, exclusions, routes, and environment keys. | Add data-driven exact assertions for every omitted record/field or narrow each source claim to the subset actually pinned. |
| F132 | major | test-gap | Catalog generation/validation negative cases have no Rust counterpart | `generate-models-strict.test.ts:15-85`; `model-data-validation.test.ts:80-176`; `reasoning-options.test.ts:4-35` | `models_generated.rs:119-158,201-233`; generator/negative validator absent | No Rust entry point recreates allowlist, schema/hash/group/timestamp/duplicate/missing-dir, reasoning-map, or strict transactional no-mutation failures. | Add a pure catalog generator/validator or xtask with transactional output and port every temporary-fixture rejection and decisive diagnostic. |
| F133 | blocking | test-gap | Generated catalog lacks compile-time literal typing counterpart | `model-catalog-types.test.ts:1-17` | `model_catalog.rs:5-7,27-48`; `models_generated.rs:1-33,316-371` | Generic `ModelCatalog<Model>` erases per-entry API/provider/ID distinctions; no compile-time XAI/Copilot route assertion exists. | Generate typed provider/model accessors or marker types with associated constants, retaining dynamic iteration, and add compile-time/static plus runtime route assertions. |
| F134 | major | test-gap | Telemetry context propagation through dispatch is untested | `telemetry-options.test.ts:74-126` | `api/simple_options.rs:166-212`; Models dispatch tests absent | Only simple-option merge checks Arc identity; direct provider and Models dispatch across four operations (eight observations) are unpinned. | Add a recording ProviderStreams double and `Arc::ptr_eq` assertions through provider/Models stream, stream_simple, fetch_deferred, and cancel_deferred. |
| F142 | blocking | wire-delta | Faux chunk boundaries diverge when UTF-16 slicing splits an astral scalar | `providers/faux.ts:269-278` | `providers/faux.rs:1194-1225,1382-1388` | Pi sizes/slices UTF-16 code units and may emit a lone high or low surrogate in a delta. Rust deliberately moves the whole scalar to one chunk, so individual events differ even though concatenated valid text matches. | Use the lossless UTF-16/WTF-8 public event-text type, slice exact code units, and serialize lone surrogates as escaped UTF-16. Port deterministic boundary vectors for splits before, within, and after astral pairs. |

## CANNOT-PRESERVE

| Delta | Technical delta | pi counterpart (file:line) | Why it cannot be preserved |
|---|---|---|---|

There are no genuine cannot-preserve rows. The submitted faux slicing candidate is F142: Rust `String` is too narrow, but the public field can idiomatically use a lossless UTF-16/WTF-8 newtype with checked conversion to `String`. That preserves pi's value and wire escaping without spawning or emulating JavaScript.

## Refuted

- **F068 — Old or untyped user messages with `content:null` cannot be normalized or read back.** Refuted. Pi `transform-messages.ts:71-74` maps nullish content to `[]`. Rust `types.rs:102-108` deserializes null as Default, `types.rs:860-863` defaults `UserContent` to `Blocks([])`, `types.rs:873-879` applies that deserializer, and `transform_messages.rs:81-84` accepts/passes it. The persisted value is already read as the same empty block collection.

## Coverage gaps and audit limits

- The pinned pi worktree omits generated `src/providers/data/*.json` payloads. The models-catalog auditor compensated by deep-comparing every Rust shard against a supplied published pi-ai 0.84.2 artifact set; the broader provider audit could not independently byte-compare those missing files.
- Generated Cloudflare model JSON imported by pi's `.models.ts` modules is absent from the pinned worktree, so those payload entries were not byte-compared with Rust's embedded Cloudflare JSON.
- Live/e2e/smoke tests were not executed or treated as hermetic parity proof. The test audit skipped live request loops and the listed smoke/e2e files, while auditing their reachable static code and hermetic portions. Owner-ruled modules/tests were excluded exactly as instructed.
- The broad idiom/impersonation audit used whole-tree pattern searches but focus-read, rather than line-read, some remaining large provider/OAuth implementations. The dedicated provider/OAuth/API auditors line-read those stated scopes and supply the findings above.
- The README location in the task's shorthand did not exist; the actual package README at `/home/vikash/pi-pin-c49906ec7/packages/ai/README.md` was read in full.
- No cargo build/test, live provider request, OAuth browser flow, or AWS request was run. This report is a static implementation plan; each package below includes the dynamic acceptance work required during implementation.

## Remediation plan

Packages are ordered by dependency. Each is intended to be handed to an implementer together with the governing goal only.

### P01 — Lossless public scalar, value, text, and ECMAScript JSON foundation

- **Findings:** F005, F012, F013, F014, F015, F018, F021, F108, F113, F142.
- **Files:** `ai/src/types.rs`; `ai/src/utils/json_parse.rs`; `ai/src/utils/error_body.rs`; new `ai/src/utils/ecma_json.rs` and `ai/src/utils/js_string.rs` (or equivalent); `ai/src/utils/mod.rs`; `ai/src/api/bedrock_converse_stream.rs`; `ai/src/providers/faux.rs`.
- **Scope:** Against pi `types.ts:350-467`, `utils/json-parse.ts:104-120`, `utils/error-body.ts:137-139`, `api/bedrock-converse-stream.ts:866-898`, and `providers/faux.ts:269-278`, replace public JS-runtime coercion and narrow integer/string assumptions with real Rust types: a binary64/non-finite dynamic number/value representation, lossless UTF-16/WTF-8 text for observable/persisted boundaries, exhaustive content unions, map-valued tool arguments, and one ECMAScript-compatible JSON serializer implementing Number spelling and integer-key ordering. Keep checked `String`/integer accessors only at internal operations. Use the text type for exact UTF-16 truncation, Bedrock sanitization, and faux slicing.
- **Acceptance:** pi type JSON round trips reject no declared shape and emit no Rust-only Null/Unknown; usage fields are numeric; JSON formatter vectors cover negative zero, 1e-7/1e-6/1e20/1e21, numeric keys, 2^53, and non-finite streaming atoms; error cap preserves split surrogate; both pi Bedrock lone-surrogate tests pass; faux emits exact surrogate-split deltas and JSON escapes them truthfully.
- **Estimate:** large.

### P02 — Typed public API, schema, catalog-key, and export surface

- **Findings:** F008, F009, F010, F011, F017, F033, F034, F040, F041, F133, F134.
- **Files:** `ai/src/lib.rs`; `ai/src/types.rs`; `ai/src/api/mod.rs`; `ai/src/utils/typebox_helpers.rs`; `ai/src/models.rs`; `ai/src/model_catalog.rs`; `ai/src/models_generated.rs`; `ai/src/providers/all.rs`; `ai/src/compat/mod.rs`.
- **Scope:** Against pi `types.ts:17-80,243-280,324-342,449-519,821-850`, `utils/typebox-helpers.ts:14-23`, `providers/all.ts:50-58`, `models.ts:156-230`, and `index.ts:1-40`, add known enums beside open identifiers; `ApiSpec`-based generic Model/Tool/ToolResult/StreamFunction/options relationships; root exports; optional ProviderStreams options and typed cancellation failure; origin-preserving ToolSchema and a root TypeBox-style typed builder; closed generated BuiltinProvider; separate read-only/mutable Models capabilities; typed generated catalog markers/accessors; telemetry-preserving dispatch.
- **Acceptance:** a Rust port of the README schema quickstart uses only root exports and no raw JSON workaround; compile-fail/pass tests pin API-option/model relationships; current known API/provider strings round-trip; every ProviderStreams operation accepts None/default and telemetry Arc identity survives the eight direct/Models calls; pi XAI/Copilot literal routing has compile-time/static and runtime assertions.
- **Estimate:** large.

### P03 — Canonical assistant event protocol and generic EventStream

- **Findings:** F001, F002, F003, F032, F038, F054, F071, F079, F094, F103, F104, F124, F125, F128, F135.
- **Files:** `ai/src/event_stream.rs`; `ai/src/providers/faux.rs`; `ai/src/api/openai_completions.rs`; `ai/src/api/openai_responses_shared.rs`; `ai/src/api/anthropic_messages.rs`; `ai/src/api/google_generative_ai.rs`; `ai/src/api/google_vertex.rs`; `ai/src/api/bedrock_converse_stream.rs`; `ai/examples/quickstart.rs`; all event consumers/tests.
- **Scope:** Against pi `utils/event-stream.ts:3-88`, `types.ts:527-551`, `README.md:130-165,574-607`, and provider emitters (including `providers/faux.ts:338-420`, `api/openai-completions.ts:336-615`, `api/anthropic-messages.ts:584-728`, and `api/bedrock-converse-stream.ts:562-727`), implement generic `EventStream<T,R>` with pi lifecycle semantics. Redefine assistant events so every nonterminal variant has `Arc<AssistantMessage> partial` and only pi's fields. Update every producer to mutate state first, attach a snapshot, and expose progressively parsed tool arguments through partial. Remove proxy reconstruction fields from the canonical event. Align faux concurrent ready polls (`providers/faux.ts:602-622`) while touching its producer.
- **Acceptance:** exact JSON shapes for all 12 event types; README quickstart and partial-tool-call examples read event.partial directly; terminal tests observe stopReason through partial; generic stream tests pin explicit result without terminal, queued drain, first terminal, late pushes, and `end(None)`; every provider/faux suite asserts snapshot content after each mutation; stateful concurrent faux polls yield pi's distinct results.
- **Estimate:** large.

### P04 — Shared mutable hook and HTTP transport contracts

- **Findings:** F060, F067, F082, F091, F096, F097, F105, F106.
- **Files:** `ai/src/types.rs`; `ai/src/api/openai_sse.rs`; `ai/src/api/openai_completions.rs`; `ai/src/api/anthropic_messages.rs`; `ai/src/api/google_generative_ai.rs`; `ai/src/api/google_vertex.rs`; `ai/src/api/bedrock_converse_stream.rs`.
- **Scope:** Against pi `types.ts:142-145`, `utils/headers.ts:3-8`, `api/openai-completions.ts:317-321`, `api/anthropic-messages.ts:565-583`, `api/google-generative-ai.ts:88-93`, `api/google-vertex.ts:106-111`, and `api/bedrock-converse-stream.ts:251-267`, replace cloned-Value OnPayload with typed live mutable payload plus optional replacement; enforce provider post-hook invariants. Carry opaque live abort handles in Google payloads. Expose typed Bedrock command values with byte bodies/options. Preserve response headers as ordered multi-values and normalize them to Fetch semantics for callbacks.
- **Acceptance:** mutation-only hooks change the request for OpenAI, Anthropic, both Google paths, and Bedrock; replacement still works; Anthropic always sends stream true; Google replacement can replace/remove the live signal used; Bedrock hook sees bytes and unset Options; OpenAI/Anthropic response hooks receive lowercased combined duplicate headers in wire order.
- **Estimate:** large.

### P05 — Validation, estimation, retry, diagnostics, and provider-error utilities

- **Findings:** F019, F020, F022, F023, F024, F025, F026, F027, F028, F029, F030, F047.
- **Files:** `ai/src/utils/{estimate,error_body,validation,provider_retry,overflow,diagnostics,node_http_proxy}.rs`; `ai/src/auth/oauth/github_copilot.rs`; P01 formatter/value modules.
- **Scope:** Against pi `utils/estimate.ts:21-107`, `error-body.ts:76-148`, `validation.ts:30-349`, `provider-retry.ts:37-66`, `overflow.ts:37-63`, `diagnostics.ts:15-30`, and `node-http-proxy.ts:92-109`, apply ECMAScript JSON/number foundations to estimates/error text; exactly port TypeBox validation messages/coercion, arbitrary-precision radix parsing, JS whitespace, ASCII regex atoms, Date.parse-compatible Retry-After handling, diagnostic thrown-value/code adapters, stable proxy errors, and retry cap formatting.
- **Acceptance:** exact snapshots for every TypeBox keyword/multi-error; all pi provider-retry vectors plus ISO/slash/local dates match; f64 integer unions and over-u128 radix values match; U+FEFF/U+0085, Unicode-digit, proxy-URL, non-Error/code, exponent, numeric-key, and token-estimate vectors match pi.
- **Estimate:** large.

### P06 — Session resources and source-preserving pluggable storage errors

- **Findings:** F006, F007, F037, F140, F141.
- **Files:** `ai/src/session_resources.rs`; `ai/src/models_store.rs`; `ai/src/models.rs`; `ai/src/auth/resolve.rs`; credential-store error wrappers as applicable.
- **Scope:** Against pi `session-resources.ts:1-24`, `models-store.ts:20-45`, `models.ts:73-76,338-429`, and `auth/resolve.ts:26-41`, implement identity-deduplicated registration/disposers and live ordered cleanup with panic/error aggregation. Redesign ModelsStore errors to separate abort from owned storage source and make public model/auth wrappers transparently source-bearing.
- **Acceptance:** duplicate callback runs once; disposer/removal/addition during cleanup follows Set traversal; every callback runs after error/panic; aggregate message and ordered sources match; an `io::Error` from read/write/delete is downcastable after refresh/publication; only an actually aborted signal is suppressed.
- **Estimate:** medium.

### P07 — Exact published model catalog and transactional generator

- **Findings:** F031, F039, F122, F123, F132.
- **Files:** `ai/src/providers/data/*`; `ai/src/models_generated.rs`; `ai/src/model_catalog.rs`; `ai/src/models_store.rs`; new generator/xtask files and catalog fixtures.
- **Scope:** Against pi `models.generated.ts:4-124`, `model-catalog.ts:22-27`, published 0.84.2 `dist/providers/data/.manifest.json`/shards, and `test/{generate-models-strict,model-data-validation,reasoning-options}.test.ts`, pin exact artifacts and regenerate MODELS; create pure strict validation/generation with transactional output; port every omitted provider assertion and correct misleading source anchors.
- **Acceptance:** manifest timestamp/hash and all 1,267 flattened models deep-equal published 0.84.2; zero extra/missing/changed shared IDs; all allowlist/schema/hash/group/timestamp/duplicate/missing-dir/reasoning-map negative fixtures fail with decisive diagnostics; strict failure leaves outputs unchanged; complete Baseten/Together/Qwen/ZAI/Fireworks/xAI/OpenAI/OpenRouter assertions pass.
- **Estimate:** large.

### P08 — Lossless Radius model publication

- **Findings:** F118, F119, F120, F131, F139.
- **Files:** `ai/src/types.rs`; `ai/src/providers/radius_config.rs`; `ai/src/providers/radius.rs`; `ai/src/models.rs`; Radius/model persistence tests.
- **Scope:** Against pi `providers/radius-config.ts:26-73` and `providers/radius.ts:67-77`, build on P01's open number/value types so Radius public helpers return `Vec<Model>` while retaining every shallow-valid model, top-level extension, nested cost/thinking-map field, modality, and unusual numeric value. Remove `filter_map` loss and overwrite only api/provider/baseUrl as pi does.
- **Acceptance:** every pi-accepted fractional, negative, large, unknown-modality, partial/custom-cost, unusual-map, and extension-bearing fixture is published and persisted; no accepted row disappears; parsed values and property presence round-trip; public helpers can be passed directly anywhere Model is accepted.
- **Estimate:** large.

### P09 — Fallible compat dispatch and truthful native CLI

- **Findings:** F035, F036, F127, F138.
- **Files:** `ai/src/compat/mod.rs`; `ai/src/bin/pi-ai.rs`; `ai/tests/compat_exports.rs`; CLI/compat docs/tests.
- **Scope:** Against pi `compat.ts:102-123,242-288` and `cli.ts:79-86`, replace mismatch/unregistered-provider panics with typed synchronous Results or terminal streams at unavoidable trait seams; propagate through complete helpers. Make CLI usage/name truthful to the Rust executable.
- **Acceptance:** mismatch and absent registration are catchable and source-bearing under both unwind/abort builds; no user input reaches panic; complete helpers use `?`; help prints native invocation and login/list behavior remains equivalent.
- **Estimate:** medium.

### P10 — OAuth runtime injection, truthful identity, loopback safety, and credential time/serde

- **Findings:** F042, F043, F048, F051, F052, F053.
- **Files:** `ai/src/auth/context.rs`; `ai/src/auth/types.rs`; `ai/src/auth/oauth/mod.rs`; all built-in OAuth flow modules, especially `github_copilot.rs`, `anthropic.rs`, `openai_codex.rs`, `radius.rs`.
- **Scope:** Against pi OAuth request sites in `auth/oauth/{anthropic,github-copilot,kimi-coding,openai-codex,openrouter,radius,xai}.ts`, Copilot identity at `github-copilot.ts:13-19,181-186`, loopback servers at `anthropic.ts:113-151`, `openai-codex.ts:335-391`, `radius.ts:174-216`, and `auth/context.ts:23-43`, add public injected OAuth HTTP runtime; replace fabricated Copilot/VS Code identity with truthful configurable identity; make loopback serving concurrent/bounded/settling; use async filesystem access; floor Date.now-style milliseconds and serialize credentials with pi order/number spelling.
- **Acceptance:** every built-in OAuth pi fetch test is recreatable with public injection and captures all requests; default traffic contains no false VS Code/extension identity; stalled sockets do not block valid callbacks; listener failures settle; expiry is integer ms; credential JSON order/number bytes match provider-produced pi objects.
- **Estimate:** large.

### P11 — OAuth provider race, permissive-success, abort, and metadata parity

- **Findings:** F044, F045, F046, F049, F050.
- **Files:** `ai/src/auth/oauth/{openrouter,anthropic,radius}.rs`; shared auth error types/tests.
- **Scope:** Against pi `auth/oauth/openrouter.ts:169-285`, `anthropic.ts:217-231,333-352`, and `radius.ts:68-167,239-397`, match OpenRouter callback-claim arbitration; use permissive raw successful payloads and pi truthiness/coercion in Anthropic/Radius; reproduce Radius's pre-abort listener behavior; retain Radius status/OAuth code in public errors.
- **Acceptance:** callback/manual race makes exactly one exchange; malformed-success matrices match pi; pre-aborted Radius does not synthesize a missed event while later abort cancels; refresh callers can inspect/downcast status and OAuth error code.
- **Estimate:** medium.

### P12 — OpenAI native SDK transport and request construction

- **Findings:** F016, F055, F058, F061, F062, F063, F064, F066, F070, F130, F136.
- **Files:** `ai/src/utils/pi_user_agent.rs`; `ai/src/api/openai_sse.rs`; `ai/src/api/openai_completions.rs`; `ai/src/api/openai_responses.rs`; `ai/Cargo.toml`; any narrow openai-oxide adapter/fork.
- **Scope:** Against pi `api/openai-completions.ts:1-13,316-335,657-746`, `api/openai-responses.ts:1-2,140-259`, `utils/pi-user-agent.ts:1-19`, and pinned OpenAI 6.40 client/streaming behavior, route Completions/Responses through openai-oxide; remove Node subprocess and fabricated JS headers; reproduce auth tombstones, SDK environment headers/precedence, timeout validation/composed abort, URL canonicalization, structured transport/body failures, and SDK SSE behavior. Preserve pi-owned user-agent truthfully.
- **Acceptance:** whole-tree search finds no Node execution or JS/Node Stainless fabrication; request captures match pi for auth removal, org/project/custom env precedence, slash boundaries, and timeout; injected fetch sees composed signal; thread events/malformed frames/body failures and retries match pinned SDK-observable outputs.
- **Estimate:** large.

### P13 — OpenAI Completions conversion, current-pin reasoning, chunks, and ordering

- **Findings:** F004, F056, F057, F059, F065, F069, F102, F121, F126, F129, F137.
- **Files:** `ai/src/types.rs`; `ai/src/api/openai_completions.rs`; `ai/src/api/transform_messages.rs`; Completions tests and serialization fixtures.
- **Scope:** Against pi `types.ts:356-364,427-447`, `api/openai-completions.ts:220-248,509-617,620-630,1131-1136,1215-1308,1503-1526`, and `test/openai-completions-reasoning-details.test.ts:115-183`, apply the reasoning-details migration, expose resolved-compat/options conversion, return setup Result from simple stream, process raw malformed chunk fields with pi truthiness/error behavior, and assemble insertion-ordered requests/messages.
- **Acceptance:** all three c499 reasoning-details tests pass in Rust terms; serialized contexts have no top-level reasoningDetails and replay current/legacy signatures correctly; direct deferred/grammar/thinking-as-text conversion tests accept explicit compat/options; missing key fails before stream; malformed-field matrix and byte/key-order captures match pi.
- **Estimate:** large.

### P14 — OpenAI Responses/Codex lifecycle, proxy, retry, and terminal edges

- **Findings:** F072, F073, F074, F075, F076, F077, F078.
- **Files:** `ai/src/api/openai_responses.rs`; `ai/src/api/openai_responses_shared.rs`; `ai/src/api/openai_codex_responses.rs`; `ai/src/api/openai_codex_responses/transport.rs`; related tests.
- **Scope:** Against pi `api/openai-responses.ts:198-215`, `api/openai-responses-shared.ts:533-600`, and `api/openai-codex-responses.ts:382-512,704-730,957-983,1354-1479`, make simple setup fallible; add proxy-aware WebSockets; retry rejected response hooks; match empty error fallback, responseId truthiness, null output, and idle close frames.
- **Acceptance:** missing credentials fail before runtime/stream; local proxy capture proves target/no-proxy and CONNECT/TLS behavior; rejected hook obeys retries/guards; empty message uses code; created/terminal ID cases match; output:null succeeds; idle peer sees code 1000/`idle_timeout` exactly.
- **Estimate:** large.

### P15 — Anthropic executable SDK client and full adapter parity

- **Findings:** F080, F081, F083, F084, F085, F086, F087, F088, F089, F090, F092, F093.
- **Files:** `ai/src/api/anthropic_messages.rs`; `ai/src/providers/anthropic.rs`; adk-anthropic adapter/dependency configuration; Anthropic tests.
- **Scope:** Against pi `api/anthropic-messages.ts:102-112,267-271,401-428,528-833,905-918,1035-1039,1337-1351` and `providers/anthropic.ts:18-38`, execute requests through adk-anthropic/public alternative-client trait; preserve open effort strings; make simple preflight fallible; correct Copilot header order, null schemas, Unicode tool names, BOM decoding, strict recognized-event validation, pinned SDK error normalization, ECMAScript request numbers, and comments.
- **Acceptance:** configured adk endpoint and alternative client own the request path; no parallel reqwest reconstruction; custom `ultra` effort is sent; preflight is Err; conflict/null/Unicode/BOM/malformed-event matrices match pi; exact HTTP/text/empty/connection/timeout error snapshots and temperature bytes pass.
- **Estimate:** large.

### P16 — Google/Vertex setup, credentials, request, and error parity

- **Findings:** F095, F098, F099, F100, F101.
- **Files:** `ai/src/api/google_shared.rs`; `ai/src/api/google_generative_ai.rs`; `ai/src/api/google_vertex.rs`; `ai/src/providers/google_vertex.rs`; credential loader modules/tests.
- **Scope:** Against pi `api/google-shared.ts:38-49`, `api/google-generative-ai.ts:280-316`, `api/google-vertex.ts:297-368,420-423,568-595`, and `providers/google-vertex.ts:49-62`, fix Pro budget table; return setup Results; normalize SDK-observable Google errors; support all pinned GoogleAuth keyFilename credential forms through pluggable filesystem access; preserve thinking-map absent/null tri-state.
- **Acceptance:** payload matrix gives Pro high 32768 and Flash high 24576; missing key/invalid map fail before stream; error snapshots match parsed compact provider bodies; accepted credential fixtures route successfully; null/undefined diagnostic strings match.
- **Estimate:** large.

### P17 — Bedrock modeled request/error path and public interceptor tests

- **Findings:** F107, F109, F110, F111.
- **Files:** `ai/src/api/bedrock_converse_stream.rs`; `ai/src/api/bedrock_converse_stream/tests.rs`; AWS SDK adapter/interceptor code.
- **Scope:** Against pi `api/bedrock-converse-stream.ts:263-269,300-390` and `test/bedrock-{custom-headers,response-headers,error-metadata}.test.ts`, filter post-hook input through modeled aws-sdk-bedrockruntime structures; preserve modeled exception object JSON; normalize raw bodies through shared trim/cap; add public-stream local-server interceptor coverage.
- **Acceptance:** unknown hook keys are absent while modeled extension fields survive; modeled exception text is compact JSON; raw body whitespace/length/non-BMP vectors match pi; local server proves signing-stage custom headers, reserved filtering, exact response headers/status, one callback, and ordering.
- **Estimate:** medium.

### P18 — Cloudflare binding and Radius/placeholder wire semantics

- **Findings:** F112, F114, F115, F116, F117.
- **Files:** `ai/src/types.rs`; `ai/src/api/cloudflare_gateway_binding.rs`; `ai/src/providers/cloudflare_stream.rs`; `ai/src/providers/radius_config.rs`; `ai/src/auth/oauth/mod.rs` request helper.
- **Scope:** Against pi `api/cloudflare-gateway-binding.ts:73-190`, `providers/cloudflare-stream.ts:10-14`, and `providers/radius-config.ts:80-94`, add Request/init composition with explicit clear/replace states and one-shot body streams; preserve header insertion/collision order; match bare query behavior and ECMAScript replacement substitutions; support bodyless GET.
- **Acceptance:** direct ports of Cloudflare binding Request/init, null override, signal/body clearing, one-shot consumption, case-collision, and bare-query tests pass; all ECMAScript dollar replacement substitutions (match, literal dollar, prefix, and suffix) match pi; Radius refresh capture has no request body/Content-Length introduced by the crate.
- **Estimate:** large.
