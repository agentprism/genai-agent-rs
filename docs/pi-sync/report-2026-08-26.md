# Owner divergence report — 2026-08-26

- Date: 2026-08-26
- Old pin: `8fa7eebd235355522c8104166b4f1f959b4e2f10`
- New pin: `e86823096c5bad39e1ca282ec24bc5eb9bec745b`
- Scoped commit count: 4 commits touch `packages/ai` or `packages/agent` (3 source/test changes and 1 changelog-only change); the full repository pin range contains 11 commits
- Changed files in port scope: 7
- Remediation performed: none

## DECISIONS-NEEDED (1)

| Changed file and verdict | Upstream change | Current port behavior | Impact | Recommendation | Effort |
| --- | --- | --- | --- | --- | --- |
| `packages/ai/src/api/openai-completions.ts` — explicit `tool_choice`: **DIVERGES**; reasoning-detail serialization: **ALREADY-CONFORMS under the adopted replay replacement** | **Tool choice:** old Pi emitted `tool_choice` only when `params.tools` was nonempty (old pin, lines 838–840); new Pi emits any explicitly supplied `toolChoice`, including when the request has no tools (new pin, lines 850–852). **Reasoning details:** old Pi reparsed and serialized the growing signature after every valid detail (old pin, lines 645–655), so intermediate partial events exposed the current signature. New Pi accumulates details in memory (new pin, lines 332–339 and 655–665), attaches them before `thinking_end` (lines 430–437), and also attaches them in `catch` before an error/abort terminal (lines 692–701). Terminal success and failure therefore retain all details observed before termination; only the intermediate mutable-partial timing and repeated parse/stringify work changed. | **Tool choice:** Rust still gates insertion on `has_active_tools` (`crates/agentprism-ai/src/openai_completions.rs:745–747`), and its focused test expects omission for explicit `ToolChoice::None` with no tools (`providers/agentprism-openai/tests/conformance.rs:107–125`). That is the old behavior. **Reasoning details:** Rust carries provider metadata through explicit replay events rather than Pi's mutable `thinkingSignature`. It keeps the pending logical detail as typed JSON and emits its latest serialized bytes on start/merge (`providers/agentprism-openai/src/decoder.rs:394–444`). Success finishes the pending replay item (`providers/agentprism-openai/src/decoder.rs:703–725`). Failure/abort terminalizes without falsely finishing it (`providers/agentprism-openai/src/decoder.rs:790–824`): the terminal envelope still contains the latest observed JSON bytes, marked `Incomplete` by the assembler (`crates/agentprism-ai/src/streaming.rs:774–793`) and therefore non-replayable, as asserted by `providers/agentprism-openai/tests/conformance.rs:987–1004`. This is the adopted Part 2 §1.2–§1.3 replacement for Pi's mutable partial, not old-behavior drift. | The request body differs from new Pi whenever a caller explicitly selects a tool policy but supplies no active tools. The changed fixture uses `toolChoice: "none"`; this is a wire-gate difference and can affect gateways that interpret an explicit policy. For reasoning details, terminal success data remains conformant and the error/abort path retains the latest observed provider bytes in both implementations; Rust's event timing and `Incomplete` representation remain the intentional architecture-level replacement. | Approve focused remediation for unconditional explicit `tool_choice` emission and update the affected Rust conformance/golden expectation. Do not change the replay protocol for the reasoning refactor: its explicit events and failure completeness already implement the adopted replacement. | Small: about 1–2 hours including focused tests and wire-suite verification. |

## ALREADY-CONFORMS

| Changed file | Upstream behavior | Current port state |
| --- | --- | --- |
| `packages/agent/src/harness/env/nodejs.ts` | Old Pi launched bare `taskkill` and did not consume its asynchronous spawn error (old pin, lines 253–264). New Pi resolves `%SystemRoot%\\System32\\taskkill.exe` directly and attaches a one-shot error listener, so abort still returns an `aborted` result when the helper cannot launch (new pin, lines 253–270). | Rust does not spawn `taskkill.exe`. Its Windows process-tree path creates and assigns a native Job Object at process spawn (`crates/agentprism-runtime-tokio/src/environment.rs:1355–1406`), calls `TerminateJobObject` for forced tree termination (`crates/agentprism-runtime-tokio/src/environment.rs:1416–1422` and `:1602–1604`), and has a Windows cancellation assertion that settles as `ProcessTermination::Cancelled` (`crates/agentprism-runtime-tokio/tests/m7_2_environment.rs:857–878`). A missing helper executable therefore cannot crash or alter the cancellation outcome. |

The already-conforming reasoning subchange is accounted for in the mixed-verdict behavioral row above so that each changed upstream file is inventoried once.

## Needs-reverification mappings

These existing mapping statuses were intentionally not changed.

| Changed TEST file | Current manifest status | Observed change and re-verification state |
| --- | --- | --- |
| `packages/ai/test/openai-completions-tool-choice.test.ts` | `semantic-parity` | The existing case changed from expecting omission to expecting `tool_choice: "none"` without tools (new pin, lines 153–174). The mapped Rust test still asserts omission (`providers/agentprism-openai/tests/conformance.rs:107–125`), so re-verification is pending the owner decision above. |
| `packages/agent/test/harness/nodejs-env.test.ts` | `semantic-parity` | Added a regression case that mocks Windows with a missing helper and expects abort to remain an error result with code `aborted` (new pin, lines 499–539). Rust's native Job Object path makes the missing-helper condition inapplicable; the mapped generic cancellation test passes on this Linux host, while the Windows-specific mapping still needs explicit cross-platform re-verification. |

## MECHANICAL inventory (3)

| Changed file | Classification basis |
| --- | --- |
| `packages/agent/CHANGELOG.md` | Release-note entry describing the Windows `taskkill.exe` fix; no executable contract. |
| `packages/ai/CHANGELOG.md` | Release-note wording changed for explicit `toolChoice`, and a reasoning-signature serialization note was added; no executable contract. |
| `packages/ai/src/types.ts` | Comment-only clarification: omitted `toolChoice` uses adapter-specific behavior instead of documenting a universal `"auto"` default. The executable change is classified with the API implementation above. |

## TEST inventory (2)

The complete TEST inventory is the two needs-reverification mappings immediately above. Both changed in place; no upstream test file was added, removed, or renamed, so no `SYNC-2026-08-26` planned mapping was added.

## OUT-OF-SCOPE inventory (0)

None. Every changed path is covered by the adopted port scope. In particular, the Node harness environment behavior falls under Architecture Part 2 §§7.10 and 10.10.

## Classification totals

| BEHAVIORAL | MECHANICAL | TEST | OUT-OF-SCOPE | Total |
| ---: | ---: | ---: | ---: | ---: |
| 2 | 3 | 2 | 0 | 7 |

The two BEHAVIORAL files contain three reviewed deltas: the explicit tool-choice behavior requires one owner decision; the Windows abort behavior already conforms; and the reasoning serialization refactor changes when Pi's mutable partial acquires replay metadata while preserving the details attached to both successful and failed/aborted terminals. Rust continues to use the adopted explicit-replay replacement: it retains the latest failure-path bytes in an `Incomplete`, non-replayable item. No crate, provider, binding, or architecture-document code was changed in this sync.

## Verification status

- The regenerated inventory contains 160 upstream test files and exactly matches `/home/vikash/pi-pin-e86823096`.
- The milestone workflow validator exits 0 with the new defaults. Its dry run reaches only `Preflight`, so it reports the existing warnings that the declared `Build` and `Closeout` phases were not exercised.
- Focused Rust checks pass for the current no-tools omission behavior, successful reasoning-detail merging, incomplete reasoning-detail failure behavior, and environment cancellation behavior.
- `PI_UPSTREAM_DIR=/home/vikash/pi-pin-e86823096 bash parity/check.sh` passes. It discovers 160 upstream test files, 252 manifest mappings (230 `semantic-parity`, 15 `deliberate-divergence`, 7 `planned`), 10 required allowlist rows, and 941 Rust tests.
- The owner-authorized prerequisite is present as commit `e6833fc`: `parity/check.py` accepts `planned` for the seven owner-ratified M10 mappings and future pi-sync additions. This sync did not modify that validator.
