[package]
name = "agentprism-env"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"
license = "MIT OR Apache-2.0"
repository = "https://github.com/agentprism/genai-agent-rs"
description = "agentprism-env: filesystem and process capability traits and portable environment types."
publish = false

[dependencies]
bytes = "1.10.1"
agentprism-ai = { path = "../agentprism-ai" }
